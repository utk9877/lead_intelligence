[project]
name = "li-core"
version = "0.1.0"
description = "Domain models, config, CIN/GSTIN identifier rules"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/li_core"]
